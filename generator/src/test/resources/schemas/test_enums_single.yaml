openapi: 3.0.3
info:
  title: Title
  description: Title
  version: 1.0.0
servers:
  - url: 'https'
paths:

components:
  schemas:
    SingleEnumItem:
      required:
        - type
        - dataType
      type: object
      properties:
        type:
          type: string
          enum:
            - ERC721
            - ERC1155
        dataType:
          type: string
          enum:
            - LEGACY
            - MODERN

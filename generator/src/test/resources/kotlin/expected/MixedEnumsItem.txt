package com.rarible.test

import kotlin.String
import kotlin.collections.List

data class MixedEnumsItem (
    val type : Type,
    val dataType : DataType,
    val features : List<Features>?,
    val modes : List<Modes>?,
) {

    enum class Type {
        ERC721,
        ERC1155,
    }

    enum class DataType {
        LEGACY,
        MODERN,
    }

    enum class Features {
        APPROVE_FOR_ALL,
        SET_URI_PREFIX,
        BURN,
        MINT_WITH_ADDRESS,
        SECONDARY_SALE_FEES,
    }

    enum class Modes {
        ONE,
        TWO,
        THREE,
    }

}

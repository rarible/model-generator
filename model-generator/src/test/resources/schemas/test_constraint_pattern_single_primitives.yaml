openapi: 3.0.3
info:
  title: Title
  description: Title
  version: 1.0.0
servers:
  - url: 'https'
paths:

components:
  schemas:
    ConstraintPatternSinglePrimitivesItem:
      required:
        - id
        - name
      type: object
      properties:
        id:
          type: integer
          format: int64
          default: 12
        name:
          type: string
          pattern: '^\d{3}-\d{2}-\d{4}$'
          default: "noname"
